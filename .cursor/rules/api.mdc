# AutoNumberGame - API и интерфейсы

## Telegram Bot API

### Основные команды

#### `/start`
**Описание**: Инициализация игры
**Ответ**: Приветственное сообщение с правилами
**Использование**: Отправляется при первом запуске бота

#### `/stats`
**Описание**: Показать статистику игры
**Ответ**: Количество найденных номеров, прогресс, топ игроков
**Использование**: Отправляется для просмотра прогресса

#### `/help`
**Описание**: Справка по командам
**Ответ**: Список доступных команд и их описание
**Использование**: Отправляется для получения помощи

#### `/reset`
**Описание**: Сброс игры (только для админов)
**Ответ**: Подтверждение сброса или отказ в доступе
**Использование**: Отправляется для перезапуска игры

### Обработка номеров

#### Формат номера
- **Паттерн**: `^\d{3}$` (ровно 3 цифры)
- **Диапазон**: 001-999
- **Примеры**: `001`, `123`, `999`

#### Ответы на номера

##### Новый номер
```
✅ Запомнили номер 001!
```

##### Повторный номер
```
❌ Номер 001 уже есть в игре
```

##### Некорректный формат
```
⚠️ Пожалуйста, отправьте ровно 3 цифры (001-999)
```

### Специальные сообщения

#### Прогресс каждые 10 номеров
```
🎯 Осталось 990 номеров!
```

#### Запрос недостающих номеров ("?")
```
🔍 Первые 10 недостающих номеров:
002, 005, 008, 012, 015, 018, 022, 025, 028, 032
```

#### Победа
```
🎉 ПОБЕДА! 🎉
Все 999 номеров найдены!
Игра завершена успешно!
```

## Внутренние интерфейсы

### GameStorage Interface

```typescript
interface GameStorage {
  // Инициализация хранилища
  initialize(): Promise<void>;
  
  // Добавление номера
  addNumber(number: string, playerId: number, playerName: string): Promise<boolean>;
  
  // Проверка существования номера
  hasNumber(number: string): Promise<boolean>;
  
  // Получение статистики
  getStats(): Promise<GameStats>;
  
  // Получение недостающих номеров
  getMissingNumbers(limit: number): Promise<string[]>;
  
  // Проверка завершения игры
  isGameComplete(): Promise<boolean>;
  
  // Сброс игры
  resetGame(): Promise<void>;
}
```

### GameLogic Interface

```typescript
interface GameLogic {
  // Обработка входящего сообщения
  processMessage(message: string, playerId: number, playerName: string): Promise<GameResponse>;
  
  // Валидация номера
  validateNumber(number: string): ValidationResult;
  
  // Генерация ответа
  generateResponse(input: string, context: GameContext): Promise<string>;
}
```

### TelegramBot Interface

```typescript
interface TelegramBot {
  // Инициализация бота
  initialize(): Promise<void>;
  
  // Запуск бота
  start(): Promise<void>;
  
  // Остановка бота
  stop(): Promise<void>;
  
  // Отправка сообщения
  sendMessage(chatId: number, message: string): Promise<void>;
  
  // Получение информации о боте
  getBotInfo(): Promise<BotInfo>;
}
```

## Структуры данных

### GameStats
```typescript
interface GameStats {
  totalNumbers: number;
  foundNumbers: number;
  remainingNumbers: number;
  progress: number; // Процент завершения
  players: PlayerStats[];
  lastUpdate: Date;
}
```

### PlayerStats
```typescript
interface PlayerStats {
  id: number;
  name: string;
  numbersFound: number;
  lastActivity: Date;
}
```

### GameResponse
```typescript
interface GameResponse {
  message: string;
  isNewNumber: boolean;
  gameComplete: boolean;
  stats?: GameStats;
}
```

### ValidationResult
```typescript
interface ValidationResult {
  isValid: boolean;
  error?: string;
  normalizedNumber?: string;
}
```

## Обработка ошибок

### Типы ошибок

#### ValidationError
- **Причина**: Некорректный формат номера
- **Обработка**: Возврат сообщения об ошибке пользователю
- **Логирование**: Уровень INFO

#### StorageError
- **Причина**: Проблемы с файловой системой
- **Обработка**: Graceful fallback, уведомление пользователя
- **Логирование**: Уровень ERROR

#### TelegramAPIError
- **Причина**: Проблемы с Telegram Bot API
- **Обработка**: Повторные попытки, уведомление пользователя
- **Логирование**: Уровень ERROR

### Стратегии восстановления

#### Автоматическое восстановление
- Повторные попытки для временных ошибок
- Fallback на локальное кэширование
- Автоматический перезапуск при критических ошибках

#### Уведомления пользователей
- Информативные сообщения об ошибках
- Предложения по решению проблемы
- Альтернативные способы взаимодействия

## Логирование

### Уровни логирования
- **DEBUG**: Детальная информация для разработчиков
- **INFO**: Общая информация о работе системы
- **WARN**: Предупреждения о потенциальных проблемах
- **ERROR**: Ошибки, требующие внимания
- **FATAL**: Критические ошибки, приводящие к остановке

### Структура логов
```json
{
  "timestamp": "2024-01-15T10:30:00.000Z",
  "level": "INFO",
  "module": "GameLogic",
  "action": "processNumber",
  "number": "001",
  "playerId": 123456,
  "result": "success"
}
```

## Мониторинг и метрики

### Ключевые метрики
- Количество активных игроков
- Скорость нахождения номеров
- Время отклика бота
- Частота ошибок

### Health Checks
- Доступность Telegram API
- Состояние файловой системы
- Память и CPU использование
- Активность бота
description:
globs:
alwaysApply: true
---
