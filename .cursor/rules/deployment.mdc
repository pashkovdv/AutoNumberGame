# AutoNumberGame - Развертывание и DevOps

## Обзор инфраструктуры

AutoNumberGame использует современный подход к развертыванию с использованием Docker контейнеризации и автоматизации через GitHub Actions.

### Архитектура развертывания

```
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│   GitHub        │    │   Docker Hub    │    │  Windows Server │
│   Repository    │───►│   Registry      │───►│      2022       │
│                 │    │                 │    │                 │
└─────────────────┘    └─────────────────┘    └─────────────────┘
         │                       │                       │
         │                       │                       │
         ▼                       ▼                       ▼
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│  GitHub Actions │    │  Docker Image   │    │  Production     │
│  CI/CD Pipeline │    │  AutoNumberGame │    │  Environment    │
└─────────────────┘    └─────────────────┘    └─────────────────┘
```

**Поток развертывания:**
1. **GitHub** - сборка и тестирование кода
2. **Docker Hub** - публикация готового образа
3. **Windows Server 2022** - загрузка и развертывание образа

## Docker контейнеризация

### Dockerfile

```dockerfile
FROM node:22-alpine

WORKDIR /app

# Копирование package файлов
COPY package*.json ./

# Установка зависимостей
RUN npm ci --only=production

# Копирование исходного кода
COPY . .

# Создание пользователя для безопасности
RUN addgroup -g 1001 -S nodejs
RUN adduser -S nodejs -u 1001

# Смена владельца файлов
RUN chown -R nodejs:nodejs /app
USER nodejs

# Открытие порта (для health checks)
EXPOSE 3000

# Health check
HEALTHCHECK --interval=30s --timeout=3s --start-period=5s --retries=3 \
  CMD node -e "process.exit(0)"

# Запуск приложения
CMD ["npm", "start"]
```

### Docker Compose

#### Продакшн конфигурация
```yaml
version: '3.8'

services:
  autonumbergame:
    build: .
    container_name: autonumbergame-prod
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - TELEGRAM_BOT_TOKEN=${TELEGRAM_BOT_TOKEN}
      - GAME_DATA_FILE=/app/data/game_data.json
      - MAX_NUMBERS=999
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
    networks:
      - autonumbergame-network
    healthcheck:
      test: ["CMD", "node", "-e", "process.exit(0)"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

networks:
  autonumbergame-network:
    driver: bridge

volumes:
  data:
  logs:
```

#### Разработка конфигурация
```yaml
version: '3.8'

services:
  autonumbergame-dev:
    build: .
    container_name: autonumbergame-dev
    restart: unless-stopped
    environment:
      - NODE_ENV=development
      - TELEGRAM_BOT_TOKEN=${TELEGRAM_BOT_TOKEN}
      - GAME_DATA_FILE=/app/data/game_data.json
      - MAX_NUMBERS=999
    volumes:
      - ./src:/app/src
      - ./data:/app/data
      - ./logs:/app/logs
      - ./node_modules:/app/node_modules
    networks:
      - autonumbergame-network
    command: npm run dev

networks:
  autonumbergame-network:
    driver: bridge
```

## GitHub Actions CI/CD

### Workflow структура

```yaml
name: CI/CD Pipeline

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  test:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [22.x]

    steps:
    - uses: actions/checkout@v4

    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v4
      with:
        node-version: ${{ matrix.node-version }}
        cache: 'npm'

    - name: Install dependencies
      run: npm ci

    - name: Run linter
      run: npm run lint

    - name: Run tests
      run: npm run test:coverage

    - name: Upload coverage to Codecov
      uses: codecov/codecov-action@v3
      with:
        file: ./coverage/lcov.info
        flags: unittests
        name: codecov-umbrella
        fail_ci_if_error: false

  build:
    needs: test
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      
      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: |
            ghcr.io/${{ github.repository }}:latest
            ghcr.io/${{ github.repository }}:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  deploy:
    needs: build
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    steps:
    - name: Deployment notification
      run: |
        echo "✅ Docker image built and pushed successfully!"
        echo "📦 Image: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest"
        echo "🔗 SHA: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.sha }}"
        echo ""
        echo "🖥️  Windows Server 2022 with Watchtower will auto-update"
        echo ""
        echo "Manual deployment:"
        echo "1. Login: docker login ${{ env.REGISTRY }} -u USERNAME -p GITHUB_TOKEN"
        echo "2. Pull: docker pull ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest"
        echo "3. Run: docker-compose up -d"
```

### Переменные окружения

#### GitHub Secrets
- `GITHUB_TOKEN` - автоматически предоставляется GitHub (для GitHub Container Registry)
- `TELEGRAM_BOT_TOKEN` - токен Telegram бота

#### Переменные окружения CI/CD
- `REGISTRY: ghcr.io` - GitHub Container Registry
- `IMAGE_NAME: ${{ github.repository }}` - имя образа на основе репозитория

## Развертывание

### Локальное развертывание

#### Требования
- Docker >= 20.10
- Docker Compose >= 2.0
- Node.js >= 22 (для разработки)

#### Шаги развертывания
```bash
# 1. Клонирование репозитория
git clone https://github.com/username/AutoNumberGame.git
cd AutoNumberGame

# 2. Настройка окружения
cp env.example .env
# Отредактировать .env файл

# 3. Запуск в Docker
docker-compose up -d

# 4. Проверка статуса
docker-compose ps
docker-compose logs -f
```

### Продакшн развертывание

#### Серверные требования (Windows Server 2022)
- **ОС**: Windows Server 2022 Standard/Datacenter
- **Docker**: Docker Desktop for Windows или Docker Engine
- **Docker Compose**: Встроенный в Docker Desktop
- **Ресурсы**: Минимум 2GB RAM, 20GB свободного места
- **Сеть**: Доступ к интернету для загрузки образов

#### Особенности Windows Server 2022
- Поддержка Windows Containers (опционально)
- Интеграция с Active Directory
- Встроенный брандмауэр Windows
- PowerShell для автоматизации
- Task Scheduler для планировщика задач

#### Автоматическое развертывание

**Для Windows Server 2022 (PowerShell):**
```powershell
# deploy.ps1
param(
    [string]$ImageTag = "latest",
    [string]$DockerHubUsername = "your-username"
)

Write-Host "🚀 Starting deployment on Windows Server 2022..." -ForegroundColor Green

try {
    # Остановка существующих контейнеров
    Write-Host "🛑 Stopping existing containers..." -ForegroundColor Yellow
    docker-compose down
    
    # Удаление старых образов
    Write-Host "🗑️ Removing old images..." -ForegroundColor Yellow
    docker image prune -f
    
    # Загрузка нового образа с Docker Hub
    Write-Host "⬇️ Pulling latest image from Docker Hub..." -ForegroundColor Yellow
    docker pull "$DockerHubUsername/autonumbergame:$ImageTag"
    
    # Запуск сервисов
    Write-Host "▶️ Starting services..." -ForegroundColor Yellow
    docker-compose up -d
    
    # Проверка здоровья
    Write-Host "🔍 Checking service health..." -ForegroundColor Yellow
    Start-Sleep -Seconds 10
    docker-compose ps
    
    Write-Host "✅ Deployment completed successfully!" -ForegroundColor Green
    
} catch {
    Write-Host "❌ Deployment failed: $($_.Exception.Message)" -ForegroundColor Red
    exit 1
}
```

**Для Linux/Ubuntu (bash):**
```bash
#!/bin/bash
# deploy.sh

set -e

echo "🚀 Starting deployment..."

# Pull latest changes
git pull origin main

# Build and start services
docker-compose -f docker-compose.yml build
docker-compose -f docker-compose.yml up -d

# Health check
echo "🔍 Checking service health..."
sleep 10
docker-compose ps

echo "✅ Deployment completed successfully!"
```

#### Мониторинг развертывания
```bash
# Проверка статуса контейнеров
docker-compose ps

# Просмотр логов
docker-compose logs -f autonumbergame

# Проверка использования ресурсов
docker stats

# Проверка health check
docker inspect autonumbergame-prod | grep -A 10 Health
```

## Настройка Windows Server 2022

### Установка Docker

#### Способ 1: Docker Desktop for Windows
```powershell
# Скачивание и установка Docker Desktop
# https://desktop.docker.com/win/main/amd64/Docker%20Desktop%20Installer.exe

# После установки перезагрузка сервера
Restart-Computer -Force
```

#### Способ 2: Docker Engine (рекомендуется для серверов)
```powershell
# Установка через PowerShell
Install-Module -Name DockerMsftProvider -Repository PSGallery -Force
Install-Package -Name docker -ProviderName DockerMsftProvider -Force

# Запуск службы Docker
Start-Service docker
Set-Service -Name docker -StartupType Automatic
```

### Настройка брандмауэра
```powershell
# Открытие портов для Docker
New-NetFirewallRule -DisplayName "Docker" -Direction Inbound -Protocol TCP -LocalPort 2375,2376 -Action Allow
New-NetFirewallRule -DisplayName "Docker Compose" -Direction Inbound -Protocol TCP -LocalPort 3000 -Action Allow
```

### Автоматизация через Task Scheduler
```powershell
# Создание задачи для автоматического обновления
$Action = New-ScheduledTaskAction -Execute "PowerShell.exe" -Argument "-File C:\Scripts\deploy.ps1"
$Trigger = New-ScheduledTaskTrigger -Daily -At 2:00AM
$Principal = New-ScheduledTaskPrincipal -UserId "NT AUTHORITY\SYSTEM" -LogonType ServiceAccount -RunLevel Highest

Register-ScheduledTask -TaskName "AutoNumberGame Update" -Action $Action -Trigger $Trigger -Principal $Principal
```

## Мониторинг и логирование

### Логирование

#### Структура логов
```
logs/
├── app.log          # Основные логи приложения
├── error.log        # Логи ошибок
├── access.log       # Логи доступа
└── archive/         # Архив логов
    ├── 2024-01-15/
    └── 2024-01-16/
```

#### Ротация логов
```yaml
# docker-compose.yml
services:
  autonumbergame:
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
```

### Мониторинг

#### Prometheus метрики
```javascript
// metrics.js
const prometheus = require('prom-client');

const httpRequestDuration = new prometheus.Histogram({
  name: 'http_request_duration_seconds',
  help: 'Duration of HTTP requests in seconds',
  labelNames: ['method', 'route', 'status_code']
});

const gameNumbersFound = new prometheus.Counter({
  name: 'game_numbers_found_total',
  help: 'Total numbers found in the game'
});
```

#### Grafana дашборд
- Количество активных игроков
- Скорость нахождения номеров
- Время отклика бота
- Использование ресурсов

## Безопасность

### Docker безопасность
- Запуск от непривилегированного пользователя
- Минимальный базовый образ (Alpine Linux)
- Сканирование уязвимостей в образе
- Обновление базовых образов

### Переменные окружения
- Хранение секретов в GitHub Secrets
- Использование .env файлов только для локальной разработки
- Шифрование чувствительных данных

### Сетевая безопасность
- Изоляция контейнеров в Docker network
- Ограничение доступа к портам
- Firewall правила на сервере

## Масштабирование

### Горизонтальное масштабирование
```yaml
# docker-compose.scale.yml
services:
  autonumbergame:
    deploy:
      replicas: 3
      resources:
        limits:
          cpus: '0.5'
          memory: 512M
        reservations:
          cpus: '0.25'
          memory: 256M
```

### Load Balancing
- Nginx reverse proxy
- Docker Swarm для оркестрации
- Kubernetes для крупных развертываний

### База данных
- Миграция с JSON на PostgreSQL
- Репликация для высокой доступности
- Backup и восстановление

## Backup и восстановление

### Стратегия backup
```bash
#!/bin/bash
# backup.sh

DATE=$(date +%Y%m%d_%H%M%S)
BACKUP_DIR="/backup/autonumbergame"

# Создание backup директории
mkdir -p $BACKUP_DIR

# Backup игровых данных
docker exec autonumbergame-prod tar czf - /app/data > $BACKUP_DIR/game_data_$DATE.tar.gz

# Backup логов
docker exec autonumbergame-prod tar czf - /app/logs > $BACKUP_DIR/logs_$DATE.tar.gz

# Удаление старых backup'ов (старше 30 дней)
find $BACKUP_DIR -name "*.tar.gz" -mtime +30 -delete

echo "Backup completed: $BACKUP_DIR/game_data_$DATE.tar.gz"
```

### Восстановление
```bash
#!/bin/bash
# restore.sh

BACKUP_FILE=$1
if [ -z "$BACKUP_FILE" ]; then
    echo "Usage: $0 <backup_file>"
    exit 1
fi

# Остановка сервиса
docker-compose stop autonumbergame

# Восстановление данных
docker exec -i autonumbergame-prod tar xzf - < $BACKUP_FILE

# Запуск сервиса
docker-compose start autonumbergame

echo "Restore completed from $BACKUP_FILE"
```

## Troubleshooting

### Частые проблемы

#### Бот не отвечает
```bash
# Проверка статуса контейнера
docker-compose ps

# Проверка логов
docker-compose logs autonumbergame

# Проверка переменных окружения
docker exec autonumbergame-prod env | grep TELEGRAM
```

#### Проблемы с данными
```bash
# Проверка прав доступа к data директории
ls -la data/

# Проверка содержимого JSON файла
docker exec autonumbergame-prod cat /app/data/game_data.json

# Восстановление прав доступа
sudo chown -R 1001:1001 data/
```

#### Проблемы с памятью
```bash
# Мониторинг использования ресурсов
docker stats

# Ограничение памяти в docker-compose.yml
services:
  autonumbergame:
    deploy:
      resources:
        limits:
          memory: 512M
```
description:
globs:
alwaysApply: true
---
